deps = [
    'umd-stdlib/core/common'
    './templates/hello-view'
]

factory = (require, com, template)->

    class HelloViewView extends com.gen.views.View
        template: template
        initUI: ->
            @$el.find('#tabs').tabs()
            return
        destroyUI: ->
            @$el.find('#tabs').tabs 'destroy'
            return